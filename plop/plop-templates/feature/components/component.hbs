interface {{pascalCase name}}ViewProps {
  title?: string
}

const {{pascalCase name}}View: React.FC<{{pascalCase name}}ViewProps> = (props) => {
  return (
    <div>
      <h2>{props.title}</h2>
    </div>
  );
};

export default {{pascalCase name}}View;
